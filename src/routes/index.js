const express = require("express");
const router = express.Router();

const userRouter = require("./userRouter");
const postRouter = require("./userRouter");
const weatherRouter = require("./weatherRouter");

module.exports = router;
