require("dotenv").config();
const axios = require("axios");

const getWeatherNow = async () => {
  try {
    const weatherInfo = "";
  } catch (error) {
    weatherInfo = undefined;
  } finally {
    return weatherInfo;
  }
};
module.exprots = {};
